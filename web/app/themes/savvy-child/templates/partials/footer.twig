<footer class="vv-footer mt-20">
    <div class=" bg-primary-800">
        <div class="container flex flex-col justify-center item-center gap-5 py-16">
            <h3 class="text-center text-white font-semibold">Looking for a VPN?</h3>
            <p class="text-white text-size_18 font-normal text-center w-2/5 mr-auto ml-auto md:text-size_18 md:w-full">Take a look at our overview of the most trustworthy, fast, and safe VPN services. Extensively tested by our experts.</p>
            {% include 'partials/button.twig' with { 
                text: 'Find the Best VPN for you!',
                url: '#',
                class: "mx-auto w-fit mt-5",
                } only
            %}
        </div>
    </div>

    <div class="container flex justify-between items-start py-14 gap-10 shrink-0 md:flex-col">
        <a class="block" href="{{home_url}}">
            <img class="block w-52" src="/app/themes/savvy-child/assets/images/ap-logo-2.png" alt='AnonymityPro Logo'>
        </a>
        <div class="flex flex-col gap-4 mt-3"> 
            <p class="font-roboto font-semibold text-dark-700 text-size_19">{{ footer_menu_1.name }}</p>
            <nav itemscope="" itemtype="https://schema.org/SiteNavigationElement">
                <ul class="flex flex-col md:gap-3">
                    {% for list in footer_menu_1.get_items %}
                        <li class="mt-5">
                            <a href="#" class="text-secondary-700 text-size_16 font-medium cursor-pointer transition duration-700 hover:text-decoration-2 hover:text-primary-700">{{list}}</a>
                        </li>
                    {% endfor %}    
                </ul>
            </nav>
        </div>
        <div class="flex flex-col gap-4 mt-3 md:hidden">
            <p class="font-roboto font-semibold text-dark-700 text-size_18 whitespace-nowrap">{{ footer_menu_2.name }}</p>
            <nav itemscope="" itemtype="https://schema.org/SiteNavigationElement">
                <ul class="flex flex-col  md:gap-3">
                    {% for list in footer_menu_2.get_items %}
                        <li class="mt-5">
                            <a href="#" class="text-secondary-700 text-size_16 whitespace-nowrap font-medium cursor-pointer decoration-2 hover:text-primary-700 transition duration-300 hover:duration-300">{{list}}</a>    
                        </li>
                    {% endfor %}   
                </ul>
            </nav>
        </div>
        <div class="flex flex-col gap-4 mt-3 sm:hidden">
            <p class="font-roboto font-semibold text-dark-700 text-size_18">{{ footer_menu_3.name }}</p>
            <nav itemscope="" itemtype="https://schema.org/SiteNavigationElement">
                <ul class="flex flex-col md:gap-3">
                {% for list in footer_menu_3.get_items %}
                    <li class="mt-5">
                        <a href="#" class="text-secondary-700 text-size_16 font-medium cursor-pointer hover:text-primary-700 transition duration-300 hover:duration-300">{{list}}</a>
                    </li>
                {% endfor %}
                </ul>
            </nav>
        </div>
        <div class="flex flex-col mt-3 gap-4">
            <p class="font-roboto font-semibold text-dark-700 text-size_18 ">{{ footer_menu_4.name }}</p>
            <nav itemscope="" itemtype="https://schema.org/SiteNavigationElement">
                <ul class="flex flex-col md:gap-3">
                {% for list in footer_menu_4.get_items %}
                    <li class="mt-5">
                        <a href="#" class="text-secondary-700 text-size_16 font-medium cursor-pointer hover:text-primary-700 transition duration-300 hover:duration-300">{{list}}</a>
                    </li>
                {% endfor %}
                </ul>
            </nav>
        </div>
    </div>
</footer>
